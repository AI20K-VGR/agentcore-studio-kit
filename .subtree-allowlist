# Kit-only paths exported by scripts/subtree-split.sh (F13). rsync --files-from=<this file>.
# Anything not listed here never leaves agentcore-studio-kit/ during a subtree export — this is
# the allowlist half of the F13 guard (the denylist half is scripts/nda-denylist.sh, a
# pre-commit hook that blocks mentor/rubric files from ever being committed into this tree in
# the first place).
pyproject.toml
uv.lock
README.md
Makefile
.env.example
.gitignore
.importlinter
.pre-commit-config.yaml
CODEOWNERS
Dockerfile
docker-compose.yml
docker-compose.test.yml
conftest.py
docker/
packages/
apps/
tests/
scripts/
docs/
.github/
.gitlab-ci.yml
deploy/
.subtree-allowlist
